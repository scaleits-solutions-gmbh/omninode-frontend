{
  "name": "omninode-frontend",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=!desk",
    "dev": "turbo run dev --filter=!desk",
    "start": "turbo run start --filter=!desk",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=18"
  }
}
